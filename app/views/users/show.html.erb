<h1>Users#show</h1>

<% @user.goals.each_with_index do |goal, n| %>
  <%= render partial: goal, locals: { goal: goal, goal_num: n } %>
<% end%>

<%= render "/goals/new" %>
<%= render @user.comments %>
<h3>Add Comment:</h3>
<%= render partial: "shared/comment_form",
  locals: { id: @user.id, type: "User"} %>